export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

export const ROUND_NUMBERS = Array.from(
  { length: 34 },
  (_, index) => index + 1
);

export const MATCHS_BY_ROUND = Array.from(
  { length: 9 },
  (_, index) => index + 1
);
